DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (used via docker-compose or production)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=webtrees
DB_USER=webtrees
DB_PASSWORD=webtrees_dev
DB_HOST=localhost
DB_PORT=5432
